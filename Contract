pragma solidity ^0.4.17;

contract OM {
    string public message;
    
    function OM(string FirstMessage) public {
        message = FirstMessage; 
    }
    
    function setMessage(string newMessage) public {
        message = newMessage;
    }
    
}
